import React from 'react'

interface HeaderProps {
    
}

export const Header = (props: HeaderProps) => {
    return (
        <header>
            <nav className="container">
                <h1 className="text-tbpp-purple">Testing</h1>
            </nav>
        </header>
    )
}
